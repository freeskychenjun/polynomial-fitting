# 多项式曲线拟合程序 - 使用说明

本程序是一个基于Windows Forms的数据拟合工具，使用多项式回归对数据进行曲线拟合，支持岭回归（L2正则化）防止过拟合。

## 系统要求

- **操作系统**: Windows 7 及以上版本
- **运行环境**: .NET 6.0 或更高版本
- **编译环境**: .NET SDK 6.0 或更高版本

## 安装与运行

### 从源码运行

```bash
# 1. 进入项目目录
cd interploation

# 2. 还原依赖并编译
dotnet build

# 3. 运行程序
dotnet run
```

### 编译可执行文件

```bash
# 编译发布版本
dotnet build -c Release

# 可执行文件位于: bin/Release/net6.0-windows/CurveFitting.exe
```

## 功能介绍

### 核心功能

- **多项式曲线拟合**: 支持 1-10 阶多项式拟合
- **自动正则化**: 根据数据特征自动选择最优正则化参数
- **自动拟合**: 改变多项式阶数时自动重新拟合，无需手动点击
- **数据导入/导出**: 支持文本文件格式的数据导入和导出
- **预测计算**: 基于拟合模型计算新的Y值
- **完整公式显示**: 在图表内显示完整的多项式表达式
- **交互式图表**: 使用 ScottPlot 4 绘制高质量图表，完美支持中文

### 新增功能 ✨

- ✅ **自动拟合**: 调整多项式阶数时自动更新拟合曲线和预测值
- ✅ **Y轴范围锁定**: 解决了Y轴范围不断增大的问题
- ✅ **完整公式显示**: 图表内显示完整拟合公式（包括λ参数和R²值）
- ✅ **中文完美支持**: 所有文本元素（标题、图例、公式）均正确显示中文

### 界面说明

```
┌─────────────────────────────────────────────────────────┐
│ [清除所有点] [拟合曲线] [加载示例数据] [导入数据] [导出数据] │
│ [输入X值计算] [导出预测数据]                             │
│ 多项式阶数: [3▼] │ □ 显示散点  □ 显示拟合曲线  □ 显示预测点 │
├─────────────────────────────────────────────────────────┤
│                                                          │
│                    图表显示区域                          │
│                                                          │
│              多项式曲线拟合                               │
│                                                          │
│         3阶多项式拟合 λ=0.125(自动) (R² = 0.9987)       │
│         y = -1234.56 + 234.56*x - 12.34*x² + ...        │
│                                                          │
│              [图例: 原始数据|拟合曲线|预测数据]           │
│                                                          │
├─────────────────────────────────────────────────────────┤
│ 原始数据: 40 | 预测数据: 10                              │
└─────────────────────────────────────────────────────────┘
```

## 使用步骤

### 1. 准备数据文件

创建一个文本文件（例如 `mydata.txt`），每行一个数据点，格式如下：

```
# 这是注释行，会被忽略
# 格式: x	y (使用制表符或逗号分隔)

16.0    4.0
16.5    25.0
17.0    42.0
17.5    100.0
18.0    186.0
```

**支持的分隔符：**
- 制表符（Tab）- **推荐**
- 逗号（`,`）
- 空格
- 中文逗号（`，`）

### 2. 导入数据

1. 点击工具栏的 **"导入数据"** 按钮
2. 选择准备好的数据文件
3. 程序会自动加载并显示散点图

**快捷方式：** 点击 **"加载示例数据"** 可以快速加载内置示例数据进行测试。

### 3. 拟合曲线

1. 在 **"多项式阶数"** 下拉框中选择阶数（1-10）
   - **低阶（1-3）**: 适合简单的线性、二次、三次关系
   - **中阶（4-6）**: 适合稍复杂的数据
   - **高阶（7-10）**: 适合复杂的数据，但需要较多数据点

2. **程序会自动拟合** 🆕
   - 改变多项式阶数时，程序会立即自动重新拟合
   - 无需再次点击"拟合曲线"按钮
   - 如果有预测点，也会自动更新

3. 程序会自动：
   - ✅ 计算最优正则化参数（防止过拟合）
   - ✅ 执行多项式回归
   - ✅ 显示拟合曲线
   - ✅ 计算并显示 R² 值（拟合优度）
   - ✅ 在图表上方显示完整拟合公式

4. 查看拟合结果：
   - **红色曲线**: 拟合的多项式曲线
   - **蓝色点**: 原始数据
   - **图表上方文本**: 完整的拟合公式和统计信息
   - **R²值**: 越接近1.0表示拟合越好

### 4. 预测新数据（可选）

如果您有X值需要计算对应的Y值：

1. 准备一个文本文件（例如 `x_values.txt`），每行一个X值：
   ```
   35.0
   35.5
   36.0
   ```

2. 点击 **"输入X值计算"** 按钮
3. 选择X值文件
4. 程序会根据当前拟合模型自动计算Y值
5. **绿色菱形点**: 显示预测数据点

6. **自动更新** 🆕：当您调整多项式阶数时，预测点会自动重新计算

7. 点击 **"导出预测数据"** 保存预测结果

### 5. 导出数据

- **导出原始数据**: 点击 **"导出数据"** 保存当前加载的数据点
- **导出预测数据**: 点击 **"导出预测数据"** 保存计算得到的预测点

导出文件格式为**制表符分隔**的文本文件，可直接用 Excel 打开。

## 多项式阶数选择指南

### 何时使用低阶多项式（1-3阶）

**适合场景：**
- 数据呈现简单的线性、抛物线或三次曲线关系
- 数据点较少（< 20个）
- 需要简单、易于解释的模型

**示例：**
- 1阶：线性增长/衰减
- 2阶：抛物线（U型或倒U型）
- 3阶：S型曲线或简单波动

### 何时使用中阶多项式（4-6阶）

**适合场景：**
- 数据关系较复杂，有多个拐点
- 数据点适中（20-50个）
- 需要平衡拟合精度和模型复杂度

### 何时使用高阶多项式（7-10阶）

**适合场景：**
- 数据关系非常复杂，有显著波动
- 数据点充足（> 50个）
- 精度要求高，允许模型复杂

**注意：** 程序会自动应用正则化防止过拟合。

## 正则化（自动）

程序会根据以下因素自动计算最优正则化参数：

| 因素 | 影响 |
|------|------|
| **多项式阶数** | 阶数越高，正则化越强 |
| **数据点数量** | 点数越多，正则化越弱 |
| **数据范围** | 范围越大，正则化适当增强 |
| **点数与阶数比** | 点数相对阶数不足时，正则化增强 |

**显示示例：**
- `λ=0.000` - 无需正则化
- `λ=0.156(自动)` - 轻微正则化
- `λ=2.500(自动)` - 强正则化

## 拟合结果说明

### R² 值（决定系数）

- **范围**: 0.0 到 1.0
- **含义**: 模型解释数据变异的比例
- **解读**:
  - 0.95-1.0: 优秀的拟合
  - 0.90-0.95: 良好的拟合
  - 0.80-0.90: 中等的拟合
  - < 0.80: 拟合不佳

### 完整拟合公式

图表上方显示的拟合公式包含两行：
1. **第一行**: 拟合参数
   - 多项式阶数
   - λ正则化参数
   - R²值

2. **第二行**: 完整多项式表达式
   - 形式: `y = a0 + a1*x + a2*x² + a3*x³ + ...`
   - 显示所有系数（保留4位小数）

## 常见问题

### Q1: 拟合曲线为什么会震荡？

**A:** 可能是多项式阶数过高导致的。尝试：
1. 降低多项式阶数
2. 确保有足够的数据点（至少为阶数的2-3倍）

### Q2: R² 值很低怎么办？

**A:** R² 低表示拟合效果不佳。尝试：
1. 增加多项式阶数
2. 检查数据是否适合多项式拟合
3. 考虑数据变换（如对数变换）

### Q3: 调整多项式阶数时为什么自动拟合？🆕

**A:** 这是程序的新功能，提升用户体验：
- 更改阶数后立即看到拟合效果
- 无需手动点击"拟合曲线"按钮
- 如果已有预测点，也会自动更新
- 如果需要手动控制，仍然可以点击"拟合曲线"按钮

### Q4: Y轴范围为什么会固定？🆕

**A:** 这是程序的优化功能：
- 防止每次调整时Y轴范围不断扩大
- 图表显示更稳定、更易读
- Y轴范围根据数据范围自动设定

### Q5: 导出的文件用什么软件打开？

**A:** 导出的文件是制表符分隔的文本文件，可以用：
- **Microsoft Excel** - 推荐
- **记事本/文本编辑器** - 查看原始内容
- **任何表格软件** - 支持TSV格式

### Q6: 如何判断拟合质量？

**A:** 综合考虑：
1. **R² 值**: 接近1.0表示拟合好
2. **曲线平滑度**: 无剧烈震荡
3. **预测合理性**: 在数据点之间的预测符合预期
4. **公式复杂度**: 在满足精度前提下，选择较低阶数

### Q7: 数据格式错误怎么办？

**A:** 确保：
- 每行只有两个数值（X和Y）
- 使用有效的分隔符（制表符、逗号、空格）
- 数值格式正确（支持小数和整数）
- 注释行以 `#` 开头

## 数据文件示例

### 示例 1: 简单线性数据
```
# 温度与压力关系
1.0    100.5
2.0    105.3
3.0    110.1
4.0    114.9
5.0    119.7
```

### 示例 2: 复杂波动数据
```
# 时间序列数据
0, 100
1, 150
2, 130
3, 180
4, 200
5, 170
```

## 技术特性

### 图表渲染

- **使用**: ScottPlot 4.1.68
- **渲染引擎**: System.Drawing（Windows原生）
- **中文支持**: 完美支持，无需额外配置
- **字体**: 自动使用系统微软雅黑字体

### 核心算法

- **多项式回归**: 最小二乘法
- **正则化**: L2正则化（岭回归）
- **矩阵求解**: 高斯消元法（部分选主元）
- **λ参数**: 自动计算最优值

### 自动拟合逻辑 🆕

```csharp
// 当用户改变多项式阶数时
if (已有散点数据)
{
    1. 自动执行拟合曲线
    2. 重新计算最优λ参数
    3. 更新拟合曲线显示
    4. 如果有预测点，自动重新计算
}
```

## 技术支持

如有问题或建议，请查看：
- **源代码**: `MainForm.cs` - 主程序逻辑
- **核心算法**: `PolynomialRegression.cs` - 多项式回归实现
- **配置文件**: `CLAUDE.md` - 开发者文档

## 版本信息

- **当前版本**: v2.0（基于 ScottPlot 4.1.68）
- **目标框架**: .NET 6.0-windows
- **许可证**: 开源（MIT License）

### 版本历史

**v2.0** (当前版本)
- ✨ 新增自动拟合功能
- ✨ 新增完整公式显示
- ✨ 修复Y轴范围问题
- ✨ 完美支持中文显示
- 🔄 降级到ScottPlot 4（更好的中文支持）

**v1.0**
- 基础多项式拟合功能
- 使用ScottPlot 5

---

**提示**: 首次使用建议点击 **"加载示例数据"** 快速体验各项功能！
